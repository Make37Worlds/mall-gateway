apiVersion: apps/v1
kind: Deployment
metadata:
  name: spring-cloud-gateway-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: spring-cloud-gateway
  template:
    metadata:
      labels:
        app: spring-cloud-gateway
    spec:
      containers:
        - name: spring-cloud-gateway
          image: your-docker-username/spring-cloud-gateway:latest
          ports:
            - containerPort: 8080
